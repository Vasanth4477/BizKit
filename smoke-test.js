const fs=require('fs');const s=fs.readFileSync('server.js','utf8');for(const x of ['DATABASE_URL','require(\'pg\')','CREATE TABLE IF NOT EXISTS users','/api/auth/signup','/api/invoices','/api/dashboard'])if(!s.includes(x))throw Error('Missing '+x);if(s.includes('better-sqlite3'))throw Error('SQLite still present');console.log('BizKit PostgreSQL smoke test passed.');
